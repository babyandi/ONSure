# ONSure local/mock provider

Deterministic local implementation of `onsure-provider-spi` for integration, policy, timeout,
rate-limit and failure tests. It performs no network access and never falls back to a different
model. It is not a production model provider.
